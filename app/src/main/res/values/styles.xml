<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ButtonTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorButtonNormal">#adfefefe</item><!--正常状态下的颜色  -->
        <item name="colorControlHighlight">#22000000</item><!--覆盖色，按下的颜色  -->
    </style>

    <style name="ButtonTheme2" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorButtonNormal">@color/btn</item><!--正常状态下的颜色  -->
        <item name="colorControlHighlight">#22000000</item><!--覆盖色，按下的颜色  -->
    </style>


    <style name="Dialog_Fullscreen">
        <item name="colorControlHighlight">@color/half</item>
        <item name="android:windowBackground">@color/half</item>
        <item name="android:windowIsTranslucent">true</item>        <!-- 设置半透明 -->
    </style>

    <color name="half">#f2000000</color>
</resources>
